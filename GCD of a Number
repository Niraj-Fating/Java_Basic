class Solution {
    public int computeGCD(int a, int b) {
        // code here
        int ans =1;
        
        for(int i = Math.min(a,b);i>=1;i--)
        {
            if((a%i)==0 && (b%i)==0)
            {
                ans=i;
                break;
            }
        }
        return ans;
    }
}
